<shortcuts xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        targetPackage must be the literal applicationId (com.xport.terminal) —
        ${applicationId} does not expand here. targetClass is the real class FQN
        (com.termux.app.TermuxActivity, unchanged by the package rename). The
        failsafe extra key must match TermuxConstants.EXTRA_FAILSAFE_SESSION
        (= TERMUX_PACKAGE_NAME + ".app.n" = "com.xport.terminal.app.n").
    -->

    <shortcut
        android:shortcutId="new_session"
        android:enabled="true"
        android:icon="@drawable/ic_new_session"
        android:shortcutShortLabel="@string/action_new_session"
        tools:targetApi="n_mr1">
        <intent
            android:action="android.intent.action.RUN"
            android:targetPackage="com.xport.terminal"
            android:targetClass="com.termux.app.TermuxActivity"
            android:name="android.shortcut.conversation"/>
    </shortcut>

    <shortcut
        android:shortcutId="new_failsafe_session"
        android:enabled="true"
        android:icon="@drawable/ic_new_session"
        android:shortcutShortLabel="@string/action_new_session_failsafe"
        tools:targetApi="n_mr1">
        <intent
            android:action="android.intent.action.RUN"
            android:targetPackage="com.xport.terminal"
            android:targetClass="com.termux.app.TermuxActivity"
            android:name="android.shortcut.conversation">
            <extra android:name="com.xport.terminal.app.n" android:value="true"/>
        </intent>
    </shortcut>

</shortcuts>
